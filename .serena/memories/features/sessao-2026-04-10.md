# Features implementadas em 2026-04-10

## Novas funcionalidades
1. **Drag-and-drop nos cards** — @dnd-kit/core + sortable. SortableRobotCard.jsx wrapper. Ordem salva no localStorage ("cardOrder").
2. **Logs expandíveis** — ExpandableLog.jsx reutilizável. Clique expande mensagem truncada. Usado em: RobotCard, RobotsPage, LogsPage, ActivityTable, Header (notificações).
3. **Notificações clicáveis** — Badge "VER LOGS" navega para /robots?selected={robotId}. Enriquecimento retroativo quando robots carrega depois.
4. **Polling na página de robôs** — /robots?selected=... atualiza automaticamente. Jobs e logs do job expandido recarregados silenciosamente.

## Bug fixes
5. **Light theme** — Overrides completos: text-white/25, border-white/15-30, bg-white/[0.03], bg-white/20, hover states.
6. **Orchestrator sem gatilhos** — orchestratorNames na TriggersPage agora combina triggers + processes.

## UX / Responsividade
7. **Modal de gatilhos responsivo** — max-h-[90vh], scroll interno, padding adaptativo.
8. **Botão Salvar fixo** — SettingsPage: barra fixa no rodapé, sempre visível.

## Performance
9. **Code splitting** — React.lazy() nas 6 páginas. Bundle: 552KB → 332KB (-40%).

## Decisões
- Webhooks UiPath adiados (requer backend público)
- Notificações simples: sem push browser, som, contador aba
- "Salve" do usuário = salvar memória, não git commit
